% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/config.R
\name{pc_save_config}
\alias{pc_save_config}
\title{Save the current configuration to .Renviron}
\usage{
pc_save_config(scope = c("user", "project"), .overwrite = FALSE)
}
\arguments{
\item{scope}{Character. Where to write the \code{.Renviron} file:
\itemize{
\item \code{"user"}: \verb{~/.Renviron}, applies to all R sessions for this user.
\item \code{"project"}: \code{./.Renviron} in the current working directory, applies
to this project only. Recommended when different projects use different
API keys.
}}

\item{.overwrite}{Logical. If \code{FALSE} (default), variables that already
exist in the target \code{.Renviron} are skipped, not replaced. Set to \code{TRUE}
to update existing entries.}
}
\value{
Invisibly returns the path to the \code{.Renviron} file that was
written, or \code{NULL} if nothing was written.
}
\description{
Writes the credentials stored by \code{\link[=pc_configure]{pc_configure()}} to a \code{.Renviron} file
so they persist across R sessions. Only variables that are currently set
in the session are written; nothing else in \code{.Renviron} is touched.
}
\details{
In interactive sessions, the function always shows exactly what it will
write (with API key values masked) and asks for confirmation before making
any changes. In non-interactive sessions it proceeds without prompting, so
use \code{.overwrite = FALSE} (the default) to avoid unintended overwrites in
scripts.
}
\section{Security}{

\code{.Renviron} stores credentials in plain text. For project-scoped files,
ensure \code{.Renviron} is listed in \code{.gitignore} before committing. The
function checks for this and warns if it is not.
}

\examples{
\dontrun{
pc_configure(email = "me@example.com", scopus_key = "my-key")
pc_save_config(scope = "project")
}
}
