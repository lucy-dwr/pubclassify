% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/search.R
\name{pc_search}
\alias{pc_search}
\title{Search bibliometric APIs for publications}
\usage{
pc_search(
  query,
  field = c("abstract", "affiliation", "funder"),
  sources = c("openalex", "scopus", "crossref"),
  max_results = 500L,
  deduplicate = TRUE,
  ...
)
}
\arguments{
\item{query}{Character. Keyword or phrase to search for. Matched against
the target \code{field} in each API.}

\item{field}{Character. The metadata field to search within. One of:
\itemize{
\item \code{"abstract"}: keyword appears in the abstract
\item \code{"affiliation"}: keyword appears in an author's institutional affiliation
\item \code{"funder"}: keyword appears in a funding acknowledgment
}}

\item{sources}{Character vector. One or more of \code{"openalex"}, \code{"scopus"},
\code{"crossref"}. Defaults to all three.}

\item{max_results}{Integer. Maximum number of records to return \emph{per
source}. Pagination is handled automatically. Default \code{500}.}

\item{deduplicate}{Logical. If \code{TRUE} (default), deduplicate across sources
by DOI after combining, preferring the richest record. See
\code{\link[=pc_deduplicate]{pc_deduplicate()}}.}

\item{...}{Additional arguments passed to the source-specific search
functions (e.g. \code{email}, \code{api_key}).}
}
\value{
A \code{\link[tibble:tibble]{tibble::tibble()}} with columns:
\describe{
\item{doi}{Digital Object Identifier (character)}
\item{title}{Article title (character)}
\item{abstract}{Article abstract; \code{NA} if unavailable (character)}
\item{year}{Publication year (integer)}
\item{authors}{Author display names (list of character vectors)}
\item{affiliations}{Institutional affiliations per author
(list of character vectors)}
\item{funders}{Funder names (list of character vectors)}
\item{journal}{Journal or venue name (character)}
\item{source}{API source: \code{"openalex"}, \code{"scopus"}, or \code{"crossref"}
(character)}
}
}
\description{
Unified interface to search OpenAlex, Scopus, and/or Crossref for
peer-reviewed publications matching a keyword in a specified field.
Results from multiple sources are automatically combined and deduplicated
by DOI.
}
\examples{
\dontrun{
# Search OpenAlex and Crossref for papers with a funder keyword
results <- pc_search(
  query   = "California Natural Resources Agency",
  field   = "funder",
  sources = c("openalex", "crossref")
)

# Search all sources for an affiliation
results <- pc_search("University of California", field = "affiliation")
}
}
