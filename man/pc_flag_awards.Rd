% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/utils_results.R
\name{pc_flag_awards}
\alias{pc_flag_awards}
\title{Flag records matching one or more award ID patterns}
\usage{
pc_flag_awards(pubs, pattern)
}
\arguments{
\item{pubs}{A \code{\link[tibble:tibble]{tibble::tibble()}} returned by any \verb{pc_search_*()} function.}

\item{pattern}{Character vector. One or more regular expressions matched
case-insensitively against each element of \code{grant_numbers}. A row is
flagged \code{TRUE} if \strong{any} grant number matches \strong{any} pattern. For
example \code{c("^4600", "^W912")} flags both DWR and USACE contracts.}
}
\value{
The input tibble with an \code{award_match} logical column appended.
Rows with no grant numbers always return \code{FALSE}.
}
\description{
Adds a logical \code{award_match} column to a results tibble, set to \code{TRUE} for
rows where any element of \code{grant_numbers} matches \strong{any} of the supplied
patterns. Useful for identifying records funded under a specific contract
structure when the funder name alone is ambiguous or inconsistently recorded.
}
\details{
Award ID patterns are often more reliable than funder name strings because
they are structured and consistent. For example, California Department of
Water Resources contracts follow a \verb{4600XXXXXX} format; U.S. Army Corps of
Engineers contracts begin with \code{W912}.
}
\examples{
\dontrun{
pubs <- pc_search_scopus("California Department of Water Resources",
                         field = "funder")
# Single pattern
pubs <- pc_flag_awards(pubs, "^4600")
# Multiple patterns
pubs <- pc_flag_awards(pubs, c("^4600", "^W912"))
pubs[pubs$award_match, ]
}
}
