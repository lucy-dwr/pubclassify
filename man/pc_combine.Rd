% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/utils_results.R
\name{pc_combine}
\alias{pc_combine}
\title{Combine results from multiple searches}
\usage{
pc_combine(..., deduplicate = TRUE)
}
\arguments{
\item{...}{Two or more tibbles returned by \code{\link[=pc_search]{pc_search()}} or source-specific
search functions, all conforming to the standard pubclassify schema.}

\item{deduplicate}{Logical. If \code{TRUE} (default), deduplicate by DOI after
combining. See \code{\link[=pc_deduplicate]{pc_deduplicate()}}.}
}
\value{
A \code{\link[tibble:tibble]{tibble::tibble()}} with the standard pubclassify schema.
}
\description{
A convenience wrapper that row-binds multiple result tibbles returned by
\code{\link[=pc_search]{pc_search()}} or source-specific search functions, then deduplicates by
DOI. Useful when running several searches (different queries or fields)
that you want to treat as a single corpus.
}
\examples{
\dontrun{
affil   <- pc_search("University of Edinburgh", field = "affiliation")
funder  <- pc_search("Wellcome Trust", field = "funder")
corpus  <- pc_combine(affil, funder)
}
}
