% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/search_crossref.R
\name{pc_search_crossref}
\alias{pc_search_crossref}
\title{Search Crossref for publications}
\usage{
pc_search_crossref(
  query,
  field = c("abstract", "affiliation", "funder"),
  max_results = 500L,
  email = NULL,
  ...
)
}
\arguments{
\item{query}{Character. Keyword or phrase to search.}

\item{field}{Character. One of \code{"abstract"}, \code{"affiliation"}, \code{"funder"}.}

\item{max_results}{Integer. Maximum number of records to return. Crossref
pages in batches of up to 1000; pagination via \code{offset} is handled
automatically. Default \code{500}.}

\item{email}{Character. Email for the Crossref polite pool. Falls back to
the value set with \code{\link[=pc_configure]{pc_configure()}}, then \code{PUBCLASSIFY_EMAIL}.}

\item{...}{Reserved for future use.}
}
\value{
A \code{\link[tibble:tibble]{tibble::tibble()}} with the standard pubclassify schema.
See \code{\link[=pc_search]{pc_search()}} for column details. \code{abstract} will be \code{NA} for many
records.
}
\description{
Queries the \href{https://api.crossref.org}{Crossref REST API} for journal
articles matching a keyword in a specified field. No API key is required;
providing an email via \code{\link[=pc_configure]{pc_configure()}} enables the polite pool for
faster, more reliable responses.
}
\details{
\strong{Abstract coverage}: Crossref abstracts are frequently absent. After
searching, consider calling \code{\link[=pc_fetch_abstracts]{pc_fetch_abstracts()}} to backfill missing
abstracts from OpenAlex using DOIs.

\strong{Funder coverage}: Crossref funder search (\code{query.funder-name}) matches
on funder names deposited by publishers and has lower coverage than
OpenAlex or Scopus. Results may be incomplete.
}
\examples{
\dontrun{
results <- pc_search_crossref("climate change", field = "abstract")
results <- pc_fetch_abstracts(results)  # backfill missing abstracts
}
}
