% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/config.R
\name{pc_configure}
\alias{pc_configure}
\title{Configure pubclassify API credentials}
\usage{
pc_configure(
  email = NULL,
  scopus_key = NULL,
  scopus_insttoken = NULL,
  llm_key = NULL,
  llm_provider = "anthropic",
  llm_base_url = NULL,
  embed_key = NULL,
  embed_provider = NULL,
  .overwrite = TRUE
)
}
\arguments{
\item{email}{Character. Your email address, used as a courtesy identifier
for OpenAlex and Crossref requests (polite pool). If \code{NULL}, falls back
to \code{PUBCLASSIFY_EMAIL}.}

\item{scopus_key}{Character. Elsevier Scopus API key. If \code{NULL}, falls back
to the \code{SCOPUS_API_KEY} environment variable.}

\item{scopus_insttoken}{Character. Elsevier institutional token, used
alongside \code{scopus_key} to authenticate off-campus COMPLETE view access.
If \code{NULL}, falls back to the \code{SCOPUS_INSTTOKEN} environment variable.
Keep this value confidential — do not commit it to version control.}

\item{llm_key}{Character. API key for the LLM provider used by
\code{\link[=pc_classify]{pc_classify()}}. If \code{NULL}, falls back to the provider-specific
environment variable (e.g. \code{ANTHROPIC_API_KEY}).}

\item{llm_provider}{Character. LLM provider name passed to \code{ellmer}
(e.g. \code{"anthropic"}, \code{"openai"}). Default \code{"anthropic"}.}

\item{llm_base_url}{Character. Base URL for an OpenAI-compatible API
endpoint. Use this when calling a non-standard provider (e.g. a local
model server or a third-party OpenAI-compatible service). If \code{NULL},
the provider's default endpoint is used. Falls back to the
\code{PUBCLASSIFY_LLM_BASE_URL} environment variable.}

\item{embed_key}{Character. API key for the embedding provider used when
\code{use_embeddings = TRUE} in \code{\link[=pc_classify]{pc_classify()}}. If \code{NULL}, falls back to
the provider-specific environment variable.}

\item{embed_provider}{Character. Embedding provider name
(e.g. \code{"openai"}, \code{"cohere"}).}

\item{.overwrite}{Logical. If \code{FALSE}, existing session values are not
replaced. Default \code{TRUE}.}
}
\value{
Invisibly returns the current configuration as a named list,
with key values masked.
}
\description{
Stores API keys and preferences for the current R session in an internal
package environment. For persistence across sessions, use
\code{\link[=pc_save_config]{pc_save_config()}} to write values to your \code{.Renviron} file.
}
\examples{
\dontrun{
pc_configure(
  scopus_key   = "my-scopus-key",
  email        = "me@example.com",
  llm_provider = "anthropic"
)
}
}
