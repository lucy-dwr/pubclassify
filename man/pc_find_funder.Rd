% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/search_openalex.R
\name{pc_find_funder}
\alias{pc_find_funder}
\title{Find funder entities in OpenAlex by name}
\usage{
pc_find_funder(query, email = NULL, max_results = 25L)
}
\arguments{
\item{query}{Character. Name or partial name of the funder to search for.}

\item{email}{Character. Email for the OpenAlex polite pool. Falls back to
the value set with \code{\link[=pc_configure]{pc_configure()}}, then the \code{PUBCLASSIFY_EMAIL}
environment variable.}

\item{max_results}{Integer. Maximum number of funder records to return.
Default \code{25}.}
}
\value{
A \code{\link[tibble:tibble]{tibble::tibble()}} with columns:
\describe{
\item{id}{OpenAlex funder entity ID. Pass this to
\code{\link[=pc_search_openalex]{pc_search_openalex()}} as \code{funder_id} to pin a specific funder.}
\item{display_name}{Full funder name as recorded in OpenAlex.}
\item{country_code}{ISO 3166-1 alpha-2 country code.}
\item{works_count}{Number of works in OpenAlex attributed to this funder.}
\item{description}{Brief description of the funder, if available.}
}
}
\description{
Searches the \href{https://docs.openalex.org/api-entities/funders}{OpenAlex Funders endpoint}
and returns a table of matching entities. Use the \code{id} column from the
result to pin a specific funder in \code{\link[=pc_search_openalex]{pc_search_openalex()}} via the
\code{funder_id} argument, avoiding ambiguous name matching.
}
\examples{
\dontrun{
# Inspect matches before searching, then pin the right one
pc_find_funder("Department of Water Resources")

pc_search_openalex(
  query     = "Department of Water Resources",
  field     = "funder",
  funder_id = "https://openalex.org/F4320332161"
)
}
}
