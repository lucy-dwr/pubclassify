% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/utils_results.R
\name{pc_fetch_acknowledgments}
\alias{pc_fetch_acknowledgments}
\title{Retrieve acknowledgment text for publications}
\usage{
pc_fetch_acknowledgments(pubs, api_key = NULL, ...)
}
\arguments{
\item{pubs}{A \code{\link[tibble:tibble]{tibble::tibble()}} returned by any \verb{pc_search_*()} function,
conforming to the standard pubclassify schema.}

\item{api_key}{Character. Scopus/Elsevier API key used for the TDM API.
Falls back to the value set with \code{\link[=pc_configure]{pc_configure()}}, then the
\code{SCOPUS_API_KEY} environment variable. If no key is found, the Elsevier
TDM step is skipped and only Europe PMC is tried.}

\item{...}{Reserved for future use.}
}
\value{
The input tibble with an \code{acknowledgment} character column
appended. Values are \code{NA} where acknowledgment text could not be
retrieved.
}
\description{
For each record with a DOI, attempts to retrieve the full acknowledgment
section from the article's full text via the Elsevier TDM API
(\verb{https://api.elsevier.com/content/article/doi/}), which returns structured
XML including a \verb{<ce:acknowledgment>} element. Requires a Scopus API key;
an institutional token (\code{SCOPUS_INSTTOKEN}) extends access to off-campus
subscriptions. Returns \code{NULL} for non-Elsevier DOIs or non-subscribed
content.
}
\details{
Records for which the Elsevier TDM API returns no text are left as \code{NA}.
}
\examples{
\dontrun{
pubs <- pc_search_scopus("California Natural Resources Agency",
                         field = "funder")
pubs <- pc_fetch_acknowledgments(pubs)
pubs[!is.na(pubs$acknowledgment), c("doi", "acknowledgment")]
}
}
