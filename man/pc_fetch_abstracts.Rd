% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/utils_results.R
\name{pc_fetch_abstracts}
\alias{pc_fetch_abstracts}
\title{Backfill metadata from OpenAlex by DOI}
\usage{
pc_fetch_abstracts(pubs, email = NULL, batch_size = 50L)
}
\arguments{
\item{pubs}{A \code{\link[tibble:tibble]{tibble::tibble()}} returned by any \verb{pc_search_*()} function,
conforming to the standard pubclassify schema.}

\item{email}{Character. Email for the OpenAlex polite pool. Falls back to
the value set with \code{\link[=pc_configure]{pc_configure()}}, then the \code{PUBCLASSIFY_EMAIL}
environment variable.}

\item{batch_size}{Integer. Number of DOIs per OpenAlex request. Default \code{50}.}
}
\value{
The input tibble with \code{abstract}, \code{authors}, \code{affiliations},
\code{grant_numbers}, and (where empty) \code{funders} filled in for records whose
DOI was found in OpenAlex.
}
\description{
For each record that has a DOI and is missing \code{abstract}, \code{authors},
\code{affiliations}, or \code{grant_numbers}, queries the
\href{https://docs.openalex.org/api-entities/works}{OpenAlex Works endpoint} in
batches to fill in those fields. Existing non-empty values are preserved.
\code{funders} is filled in from OpenAlex awards only when the field is empty.
}
\details{
This is the recommended way to enrich Scopus results retrieved without
COMPLETE view access, or Crossref results which rarely include abstracts.
}
\examples{
\dontrun{
pubs <- pc_search_scopus("California Natural Resources Agency",
                         field = "funder")
pubs <- pc_fetch_abstracts(pubs)
}
}
