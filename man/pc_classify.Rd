% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/classify.R
\name{pc_classify}
\alias{pc_classify}
\title{Classify publications into a scholarly field taxonomy}
\usage{
pc_classify(
  pubs,
  taxonomy,
  provider = "anthropic",
  model = NULL,
  api_key = NULL,
  base_url = NULL,
  system_prompt = NULL,
  classify_instructions = NULL,
  use_embeddings = FALSE,
  embed_provider = NULL,
  embed_key = NULL,
  top_k = 5L,
  batch_size = 10L,
  ...
)
}
\arguments{
\item{pubs}{A \code{\link[tibble:tibble]{tibble::tibble()}} returned by \code{\link[=pc_search]{pc_search()}} or
\code{\link[=pc_combine]{pc_combine()}}, containing at minimum \code{title} and \code{abstract} columns.}

\item{taxonomy}{A \code{\link[=pc_taxonomy]{pc_taxonomy()}} object defining the fields to classify
into.}

\item{provider}{Character. LLM provider passed to \code{ellmer}. One of
\code{"anthropic"}, \code{"openai"}, or \code{"openai-compatible"}. Use
\code{"openai-compatible"} for any OpenAI-compatible gateway (e.g. a
multi-model proxy) that implements the \verb{/chat/completions} endpoint but
not the newer OpenAI Responses API. Default \code{"anthropic"}.}

\item{model}{Character. Model identifier. If \code{NULL}, uses the provider
default as determined by \code{ellmer}.}

\item{api_key}{Character. API key for the LLM provider. Falls back to the
value set with \code{\link[=pc_configure]{pc_configure()}}, then the \code{PUBCLASSIFY_LLM_KEY}
environment variable, then the provider's own default variable
(e.g. \code{OPENAI_API_KEY}).}

\item{base_url}{Character. Base URL for an OpenAI-compatible API endpoint.
Only used when \code{provider = "openai"}. Falls back to the value set with
\code{\link[=pc_configure]{pc_configure()}}, then the \code{PUBCLASSIFY_LLM_BASE_URL} environment
variable. If still \code{NULL}, the standard OpenAI endpoint is used.}

\item{system_prompt}{Character. Replaces the default system message sent to
the LLM. The default instructs the model to act as a scientific literature
classifier and return structured JSON. Override this to change the model's
persona or task framing. The output format instruction is always appended
after this value.}

\item{classify_instructions}{Character. Optional additional instructions
injected into the user message after the taxonomy definitions and before
the publications. Use this for domain-specific guidance such as priority
rules, tie-break logic, or category-specific signals.}

\item{use_embeddings}{Logical. If \code{TRUE}, retrieves \code{top_k} candidate
fields via embedding similarity before the LLM call. Requires
\code{embed_provider} and a valid API key. Default \code{FALSE}.}

\item{embed_provider}{Character. Embedding provider
(e.g. \code{"openai"}, \code{"cohere"}). Required when \code{use_embeddings = TRUE}.}

\item{embed_key}{Character. Embedding API key. Falls back to the
provider-specific environment variable when \code{NULL}.}

\item{top_k}{Integer. Number of candidate taxonomy fields passed to the LLM
per publication when \code{use_embeddings = TRUE}. Default \code{5}.}

\item{batch_size}{Integer. Number of publications per LLM call. Batching
reduces the number of API round-trips for large result sets. Default \code{10}.}

\item{...}{Additional arguments passed to the \code{ellmer} chat constructor
(e.g. \code{seed}).}
}
\value{
The input tibble with four additional columns:
\describe{
\item{pc_field}{Assigned taxonomy field name (character)}
\item{pc_rationale}{Brief LLM reasoning for the assignment (character)}
\item{pc_text_source}{Text used for classification:
\code{"title+abstract"} or \code{"title"} (character)}
\item{pc_classified_by}{Method used: \code{"llm-full"} (full taxonomy) or
\code{"llm-rag"} (embedding-narrowed) (character)}
}
}
\description{
Uses a large language model (via \code{ellmer}) to assign each publication in
\code{pubs} to a field in \code{taxonomy}. Classification text is built as
\code{title + abstract} when an abstract is present, or \code{title} alone when it
is not (see \code{\link[=pc_fetch_abstracts]{pc_fetch_abstracts()}} to backfill missing abstracts first).
}
\details{
By default (\code{use_embeddings = FALSE}), the full taxonomy is included in
every LLM prompt. For taxonomies with 40 or more fields, set
\code{use_embeddings = TRUE} to first narrow candidates via embedding similarity,
passing only the top \code{top_k} fields to the LLM per publication.
}
\examples{
\dontrun{
tax  <- pc_taxonomy_example()
pubs <- pc_search("University of Edinburgh", field = "affiliation",
                  sources = "openalex")
pubs <- pc_classify(pubs, tax, provider = "openai", model = "gpt-4o")
}
}
