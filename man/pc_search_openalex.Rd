% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/search_openalex.R
\name{pc_search_openalex}
\alias{pc_search_openalex}
\title{Search OpenAlex for publications}
\usage{
pc_search_openalex(
  query,
  field = c("abstract", "affiliation", "funder"),
  max_results = 500L,
  email = NULL,
  funder_id = NULL,
  ...
)
}
\arguments{
\item{query}{Character. Keyword or phrase to search.}

\item{field}{Character. One of \code{"abstract"}, \code{"affiliation"}, \code{"funder"}.}

\item{max_results}{Integer. Maximum number of records to return. OpenAlex
uses cursor-based pagination in batches of up to 200; this is handled
automatically. Default \code{500}.}

\item{email}{Character. Email for the OpenAlex polite pool. Falls back to
the value set with \code{\link[=pc_configure]{pc_configure()}}, then the \code{PUBCLASSIFY_EMAIL}
environment variable. \code{NA} if none is found (still works through the slower
pool).}

\item{funder_id}{Character. One or more OpenAlex funder entity IDs
(e.g. \code{"https://openalex.org/F4320308027"}). When provided with
\code{field = "funder"}, skips the name-based lookup entirely and filters works
directly by these IDs. Use \code{\link[=pc_find_funder]{pc_find_funder()}} to discover IDs. Ignored
when \code{field} is not \code{"funder"}.}

\item{...}{Reserved for future use.}
}
\value{
A \code{\link[tibble:tibble]{tibble::tibble()}} with the standard pubclassify schema.
See \code{\link[=pc_search]{pc_search()}} for column details.
}
\description{
Queries the \href{https://docs.openalex.org}{OpenAlex API} for peer-reviewed
works matching a keyword in a specified field. No API key is required;
providing an email via \code{\link[=pc_configure]{pc_configure()}} or the \code{email} argument places
requests in the polite pool for better rate limits and higher quotas.
}
\examples{
\dontrun{
pc_search_openalex("University of California", field = "affiliation")

# Disambiguate first, then pin the exact funder
pc_find_funder("Department of Water Resources")
pc_search_openalex(
  query     = "Department of Water Resources",
  field     = "funder",
  funder_id = "https://openalex.org/F4320308027"
)
}
}
