% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/search_scopus.R
\name{pc_search_scopus}
\alias{pc_search_scopus}
\title{Search Scopus for publications}
\usage{
pc_search_scopus(
  query,
  field = c("abstract", "affiliation", "funder"),
  max_results = 500L,
  api_key = NULL,
  auto_fetch = TRUE,
  email = NULL,
  start_year = NULL,
  end_year = NULL,
  doc_type = NULL,
  award_pattern = NULL,
  ...
)
}
\arguments{
\item{query}{Character. Keyword or phrase to search. The value is embedded
in a Scopus Query Language expression, e.g. \code{ABS("climate change")}.}

\item{field}{Character. One of \code{"abstract"}, \code{"affiliation"}, \code{"funder"}.}

\item{max_results}{Integer. Maximum number of records to return. Scopus
pages in batches of 25; pagination via \code{start} offset is handled
automatically. The Scopus Search API caps accessible results at 5,000
regardless of total result count. Default \code{500}.}

\item{api_key}{Character. Scopus API key. Falls back to the value set with
\code{\link[=pc_configure]{pc_configure()}}, then the \code{SCOPUS_API_KEY} environment variable.}

\item{auto_fetch}{Logical. If \code{TRUE} (default), automatically calls
\code{\link[=pc_fetch_abstracts]{pc_fetch_abstracts()}} after searching to backfill \code{authors}, \code{abstract},
\code{affiliations}, and \code{grant_numbers} via OpenAlex for any records where
those fields are empty. Most useful when the Scopus COMPLETE view is not
available (e.g. off-campus access). Set to \code{FALSE} to skip the backfill.}

\item{email}{Character. Email for the OpenAlex polite pool used by
\code{\link[=pc_fetch_abstracts]{pc_fetch_abstracts()}} when \code{auto_fetch = TRUE}. Falls back to the value
set with \code{\link[=pc_configure]{pc_configure()}}, then the \code{PUBCLASSIFY_EMAIL} environment
variable.}

\item{start_year}{Integer. Earliest publication year to include (inclusive).
Appended to the query as \verb{PUBYEAR AFT <start_year - 1>}. \code{NULL} (default)
applies no lower year bound.}

\item{end_year}{Integer. Latest publication year to include (inclusive).
Appended to the query as \verb{PUBYEAR BEF <end_year + 1>}. \code{NULL} (default)
applies no upper year bound.}

\item{doc_type}{Character vector. One or more document types to restrict
results. Accepted values: \code{"article"}, \code{"review"}, \code{"conference_paper"},
\code{"book"}, \code{"book_chapter"}, \code{"letter"}, \code{"editorial"}, \code{"short_survey"}.
Multiple values are combined with OR. \code{NULL} (default) returns all
document types.}

\item{award_pattern}{Character vector. One or more optional regular
expressions matched case-insensitively against \code{grant_numbers} to append
an \code{award_match} logical column. A record is flagged if any grant number
matches any pattern. Award IDs are often more reliable than funder name
strings for identifying specific funding programmes. For example,
\code{"^4600"} flags California DWR contracts; \code{c("^4600", "^W912")} flags
both DWR and U.S. Army Corps contracts. \code{NULL} (default) skips flagging.
See \code{\link[=pc_flag_awards]{pc_flag_awards()}}.}

\item{...}{Reserved for future use.}
}
\value{
A \code{\link[tibble:tibble]{tibble::tibble()}} with the standard pubclassify schema.
See \code{\link[=pc_search]{pc_search()}} for column details.
}
\description{
Queries the
\href{https://dev.elsevier.com/documentation/ScopusSearchAPI.wadl}{Elsevier Scopus Search API}
for peer-reviewed works matching a keyword in a specified field using
Scopus Query Language (SQL). A Scopus API key is required; institutional
access is typically needed for full metadata retrieval.
}
\section{COMPLETE view and institutional access}{

This function requests the Scopus Search API COMPLETE view, which returns
author names (\code{dc:creator} + \code{author} array), an abstract snippet
(\code{dc:description}), and funding metadata (\code{fund-sponsor}, \code{fund-no}).
The COMPLETE view requires an institutional Scopus subscription; requests
from non-subscriber API keys will receive only the STANDARD view fields
(title, DOI, journal, date, affiliation) and an HTTP 401/403 error.

Note that \code{dc:description} is a short snippet, not the full abstract, and
funding metadata may be sparse. Use \code{\link[=pc_fetch_abstracts]{pc_fetch_abstracts()}} to backfill
full abstracts and funding data via OpenAlex after searching.
}

\examples{
\dontrun{
pc_search_scopus("California Natural Resources Agency", field = "funder")
pc_search_scopus("University of California", field = "affiliation")
}
}
